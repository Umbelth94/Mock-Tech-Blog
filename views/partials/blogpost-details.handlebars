<section id="blogpost-card">
    <a href="/blogpost/{{id}}">
        <header>{{title}}</header>
        <p>{{body_content}}</p>
        <p>{{date_created}}</p>
        <p>{{user.name}}</p>
    </a>
</section>